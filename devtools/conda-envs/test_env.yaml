name: test
channels:
- conda-forge
- omnia
dependencies:
    # Base depends
  - python
  - pip
  - pandas
  - requests
  - pint
  - appdirs
  - openforcefield

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

