name: test
channels:
- conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - pandas
  - requests
  # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

