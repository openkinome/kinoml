name: test
channels:
dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - pytest
  - pytest-cov
  - codecov

  # Docs
  - pip:
    - mkdocs==1.0.4
    - mkautodoc==0.7.1
    - mkdocs-material
    - mkdocs-minify-plugin
    - pymdown-extensions
